# 🎯 Prompt احترافي كامل لـ Replit Agent

## 📋 انسخ هذا والصقه في Replit Agent

---

```
# PROJECT: SEOMaster Pro - SaaS Backend

## CONTEXT
You are a senior backend engineer. I imported a Next.js 14 project from GitHub. 
I need you to FOCUS ONLY ON THE BACKEND. No landing pages, no UI, no dashboards.

## CURRENT STATE
- Next.js 14 with App Router
- TypeScript
- Some files exist but are incomplete
- Node.js 20 (Replit default)

## GOAL
Build a production-ready SaaS backend for an SEO analysis platform with:
- User authentication (Supabase)
- Article SEO analysis API (OpenAI)
- Credit/usage system
- Stripe subscription payments
- Webhook handling

---

## STEP 1: ENVIRONMENT SETUP

First, fix the environment. Execute in Shell:

1. Upgrade Node to 22:
   - Check current: node --version
   - If below 22, install Node 22 using available method in Replit

2. Install ALL required dependencies:
   npm install @supabase/supabase-js @supabase/ssr
   npm install openai stripe zod
   npm install -D @types/node

3. If npm install fails with E403 (Replit blocks packages):
   - Report the blocked packages
   - Suggest alternatives or workarounds
   - DO NOT proceed until dependencies install

4. Verify installation:
   npm list @supabase/supabase-js openai stripe

---

## STEP 2: PROJECT STRUCTURE

Create/verify this EXACT structure. Delete anything not in this list:

```
app/
├── layout.tsx                 # Root layout (minimal)
├── globals.css                # Tailwind base
├── api/
│   ├── auth/
│   │   ├── callback/route.ts  # OAuth callback
│   │   ├── signup/route.ts    # Email signup
│   │   ├── signin/route.ts    # Email signin
│   │   └── signout/route.ts   # Signout
│   ├── user/
│   │   ├── me/route.ts        # Current user
│   │   └── usage/route.ts     # Usage stats
│   ├── analyze/
│   │   └── route.ts           # SEO analysis
│   └── stripe/
│       ├── checkout/route.ts  # Create checkout
│       ├── portal/route.ts    # Billing portal
│       └── webhook/route.ts   # Stripe webhooks
├── (delete everything else in app/)

lib/
├── supabase/
│   ├── client.ts              # Browser client
│   ├── server.ts              # Server client
│   ├── admin.ts               # Service role client
│   └── middleware.ts          # Session refresh
├── stripe/
│   ├── client.ts              # Stripe SDK
│   ├── plans.ts               # Plan config
│   └── helpers.ts             # Customer/subscription helpers
├── seo-agent.ts               # AI agent (OpenAI)
├── openai.ts                  # OpenAI client
├── rate-limit.ts              # Rate limiting
├── credits.ts                 # Credits system
├── utils.ts                   # Helpers
└── types.ts                   # Shared types

types/
└── database.ts                # DB types

middleware.ts                  # Root middleware
.env.local                     # Secrets
.env.example                   # Template
```

DELETE these if they exist:
- app/page.tsx (no landing page)
- components/landing/ (no UI)
- app/dashboard/ (no dashboard UI)
- app/(auth)/ (no auth UI)
- app/pricing/ (no pricing UI)

---

## STEP 3: SUPABASE DATABASE SCHEMA

Create `supabase/schema.sql` with EXACTLY this content.
Then tell me to run it in Supabase SQL Editor.

```sql
-- Enable UUID extension
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- ============================================
-- PROFILES
-- ============================================
CREATE TABLE IF NOT EXISTS profiles (
  id UUID PRIMARY KEY REFERENCES auth.users ON DELETE CASCADE,
  email TEXT UNIQUE NOT NULL,
  full_name TEXT,
  avatar_url TEXT,
  plan TEXT NOT NULL DEFAULT 'free' CHECK (plan IN ('free','pro','enterprise')),
  credits INTEGER NOT NULL DEFAULT 5,
  stripe_customer_id TEXT UNIQUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Auto-create profile on signup
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email, full_name, avatar_url)
  VALUES (
    NEW.id,
    NEW.email,
    NEW.raw_user_meta_data->>'full_name',
    NEW.raw_user_meta_data->>'avatar_url'
  )
  ON CONFLICT (id) DO NOTHING;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();

-- ============================================
-- ANALYSES
-- ============================================
CREATE TABLE IF NOT EXISTS analyses (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  content TEXT NOT NULL,
  url TEXT,
  score INTEGER CHECK (score >= 0 AND score <= 100),
  results JSONB NOT NULL,
  tokens_used INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_analyses_user ON analyses(user_id, created_at DESC);

-- ============================================
-- SUBSCRIPTIONS
-- ============================================
CREATE TABLE IF NOT EXISTS subscriptions (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  stripe_subscription_id TEXT UNIQUE NOT NULL,
  stripe_price_id TEXT NOT NULL,
  plan TEXT NOT NULL,
  status TEXT NOT NULL,
  current_period_start TIMESTAMPTZ,
  current_period_end TIMESTAMPTZ,
  cancel_at_period_end BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_subscriptions_user ON subscriptions(user_id);
CREATE INDEX IF NOT EXISTS idx_subscriptions_stripe ON subscriptions(stripe_subscription_id);

-- ============================================
-- USAGE LOGS
-- ============================================
CREATE TABLE IF NOT EXISTS usage_logs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  action TEXT NOT NULL,
  tokens_used INTEGER DEFAULT 0,
  cost_usd NUMERIC(10,6) DEFAULT 0,
  metadata JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_usage_user_date ON usage_logs(user_id, created_at DESC);

-- ============================================
-- ROW LEVEL SECURITY
-- ============================================
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
ALTER TABLE analyses ENABLE ROW LEVEL SECURITY;
ALTER TABLE subscriptions ENABLE ROW LEVEL SECURITY;
ALTER TABLE usage_logs ENABLE ROW LEVEL SECURITY;

DROP POLICY IF EXISTS "Users view own profile" ON profiles;
CREATE POLICY "Users view own profile" ON profiles
  FOR SELECT USING (auth.uid() = id);

DROP POLICY IF EXISTS "Users update own profile" ON profiles;
CREATE POLICY "Users update own profile" ON profiles
  FOR UPDATE USING (auth.uid() = id);

DROP POLICY IF EXISTS "Users manage own analyses" ON analyses;
CREATE POLICY "Users manage own analyses" ON analyses
  FOR ALL USING (auth.uid() = user_id);

DROP POLICY IF EXISTS "Users view own subscriptions" ON subscriptions;
CREATE POLICY "Users view own subscriptions" ON subscriptions
  FOR SELECT USING (auth.uid() = user_id);

DROP POLICY IF EXISTS "Users view own usage" ON usage_logs;
CREATE POLICY "Users view own usage" ON usage_logs
  FOR SELECT USING (auth.uid() = user_id);

-- ============================================
-- TRIGGERS
-- ============================================
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = NOW();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS profiles_updated_at ON profiles;
CREATE TRIGGER profiles_updated_at
  BEFORE UPDATE ON profiles
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

DROP TRIGGER IF EXISTS subscriptions_updated_at ON subscriptions;
CREATE TRIGGER subscriptions_updated_at
  BEFORE UPDATE ON subscriptions
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();
```

---

## STEP 4: ENVIRONMENT VARIABLES

Create `.env.example`:

```
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI
OPENAI_API_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICE_PRO=
STRIPE_PRICE_ENTERPRISE=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

Then tell me to add these in Replit Secrets (Tools → Secrets).

---

## STEP 5: SUPABASE CLIENTS

Create these files with full production code:

### lib/supabase/client.ts
- createBrowserClient from @supabase/ssr
- Reads NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY

### lib/supabase/server.ts
- createServerClient from @supabase/ssr
- Uses cookies() from next/headers
- Handles getAll and setAll properly

### lib/supabase/admin.ts
- createClient with SERVICE_ROLE_KEY
- Only for admin operations (never expose to client)

### lib/supabase/middleware.ts
- updateSession() function
- Refreshes auth session on every request
- Returns NextResponse

### middleware.ts (root)
- Calls updateSession()
- Protects /api/* except /api/auth/*
- Config matcher

---

## STEP 6: SHARED UTILITIES

### lib/types.ts
- Database types (Profile, Analysis, Subscription, UsageLog)
- API response types (ApiResponse<T>, ApiError)
- Plan types

### lib/utils.ts
- cn() for className merging
- formatDate()
- formatNumber()
- sleep()

### lib/rate-limit.ts
- Simple in-memory rate limiter
- Map<string, { count, resetAt }>
- rateLimit(userId, limit, windowMs)

### lib/credits.ts
- checkCredits(userId): Promise<number>
- deductCredits(userId, amount): Promise<void>
- addCredits(userId, amount): Promise<void>
- Uses admin client for atomic updates

### lib/openai.ts
- OpenAI client instance
- Export default

### lib/seo-agent.ts
- analyzeArticle(content: string): Promise<SEOAnalysis>
- generateKeywords(topic: string): Promise<string[]>
- Full types: SEOAnalysis, Issue
- Uses GPT-4o-mini
- Retry logic (3 attempts)
- 30s timeout
- JSON response format

---

## STEP 7: API ROUTES

Create these with full production code:

### app/api/auth/callback/route.ts
- GET: handle OAuth callback
- exchangeCodeForSession
- Redirect to APP_URL

### app/api/auth/signup/route.ts
- POST: { email, password, full_name }
- Validate with Zod
- supabase.auth.signUp()
- Return user or error

### app/api/auth/signin/route.ts
- POST: { email, password }
- supabase.auth.signInWithPassword()
- Return session

### app/api/auth/signout/route.ts
- POST: sign out
- Clear cookies

### app/api/user/me/route.ts
- GET: current user + profile
- 401 if not authenticated

### app/api/user/usage/route.ts
- GET: usage stats
- Return: plan, credits_left, total_analyses, recent_analyses

### app/api/analyze/route.ts
- POST: { content, url? }
- Validate with Zod (content min 100 chars)
- Check auth → 401
- Check credits → 402
- Rate limit → 429
- Call analyzeArticle()
- Save to analyses table
- Deduct credit
- Log usage
- Return result

### app/api/stripe/checkout/route.ts
- POST: { priceId }
- Auth required
- getOrCreateCustomer()
- Create checkout session
- Return { url }

### app/api/stripe/portal/route.ts
- POST: create billing portal session
- Return { url }

### app/api/stripe/webhook/route.ts
- POST: handle webhooks
- Verify signature
- Handle events:
  - checkout.session.completed
  - customer.subscription.updated
  - customer.subscription.deleted
  - invoice.payment_failed
- Update profiles + subscriptions
- Idempotent

---

## STEP 8: STRIPE

### lib/stripe/client.ts
- Stripe SDK instance

### lib/stripe/plans.ts
- PLAN_CONFIG with free/pro/enterprise
- credits, price_id, name

### lib/stripe/helpers.ts
- getOrCreateCustomer(userId, email)
- syncSubscription(stripeSub)
- updateUserPlan(userId, plan, credits)

---

## REQUIREMENTS FOR ALL CODE

1. TypeScript strict mode
2. Zod validation on all POST bodies
3. Proper HTTP status codes:
   - 200 OK
   - 400 Bad Request (validation)
   - 401 Unauthorized
   - 402 Payment Required (no credits)
   - 404 Not Found
   - 429 Too Many Requests
   - 500 Internal Server Error
4. Response format:
   - Success: { data: T }
   - Error: { error: string, code?: string }
5. Try/catch on every route
6. console.error for debugging
7. No UI, no HTML, JSON only
8. Comments for complex logic

---

## WORKFLOW

Execute in this order, ONE STEP AT A TIME:

1. Run STEP 1 (environment setup). Report result.
2. Run STEP 2 (clean structure). Report what was deleted.
3. Create STEP 3 (schema.sql). Tell me to run it in Supabase.
4. Create STEP 4 (.env.example). Tell me to add secrets.
5. Create STEP 5 (Supabase clients). Show me each file.
6. Create STEP 6 (utilities). Show me each file.
7. Create STEP 7 (API routes). Show me each file.
8. Create STEP 8 (Stripe). Show me each file.
9. Test: run `npm run dev` and report.
10. Test each API endpoint with curl.

WAIT for my confirmation after EACH step before proceeding.

Do NOT create UI, landing pages, dashboards, or components.

Start with STEP 1 now.
```

---

## 🎯 كيف تستخدم هذا Prompt

### 📍 الطريقة الصحيحة:

```
1. افتح Replit → Agent
2. الصق الـ Prompt كاملاً
3. اضغط Enter
4. انتظر → سينفذ STEP 1
5. راجع النتيجة
6. قل: "Continue to STEP 2"
7. كرر...
```

### ⚠️ إذا توقف Agent:

```
قل: "Continue from where you stopped"
أو: "Execute STEP X"
```

### ⚠️ إذا فشل npm install (E403):

```
قل: "npm install failed with E403. 
Find alternative: use yarn, pnpm, or manually download packages."
```

---

## 🎯 نقاط القوة في هذا الـ Prompt

### ✅ ما يفعله:

```
1. يحدد الهدف بوضوح: Backend only
2. يمنع UI تماماً
3. يحذف ما لا نحتاجه
4. يعطي schema كامل
5. يعطي prompts لـ env
6. يرتب الملفات منطقياً
7. يطلب production-ready code
8. يضع شروط واضحة (Zod, status codes)
9. يقول "WAIT after each step"
10. يطلب اختبار في النهاية
```

### ✅ ما يمنعه:

```
❌ UI components
❌ Landing pages
❌ Dashboard
❌ تشتت
❌ كود ناقص
❌ تجاهل الأخطاء
```

---

## 🚀 ابدأ الآن

### الخطوة 1:
```
1. افتح Replit
2. اذهب لـ Agent
3. الصق الـ Prompt كاملاً
4. اضغط Enter
```

### الخطوة 2:
```
أرسل لي:
- ماذا فعل Agent في STEP 1
- أي أخطاء ظهرت
- صورة للشاشة
```

---

## 💡 نصائح مهمة

### إذا Replit Free يحجب الحزم:

```
في الـ Prompt، أضف:
"If npm install fails due to E403, 
try: yarn add, or pnpm add, or 
download packages manually."
```

### إذا Agent لا يلتزم:

```
كرر:
"STOP. Backend only. No UI. 
Continue with STEP X."
```

### إذا توقف Agent:

```
"Continue from STEP X"
```

---

## 🎯 بعد أن ينتهي

سيكون لديك:

```
✅ 12 API endpoint يعمل
✅ Supabase كامل
✅ SEO Agent حقيقي
✅ Stripe مربوط
✅ Webhooks
✅ Rate limiting
✅ Credits system
✅ Admin client
✅ Middleware
✅ Types كاملة
```

**Backend احترافي جاهز للإنتاج!**

---

## 📞 أرسل لي

```
1. هل الصقت الـ Prompt؟
2. ماذا فعل Agent؟
3. أي أخطاء؟
4. صورة للشاشة
```

**وسأوجهك خطوة بخطوة! 💪**